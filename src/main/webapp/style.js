/*    function drawSectionHeader( Canvas, Section ){
    var cvs = document.querySelector( Canvas, Section );
    var graph = cvs.getContext( "2d" );
    graph.strokeStyle="#000";
    graph.lineWidth = 5;
    graph.beginPath();
    graph.moveTo(20, 0);
    graph.lineTo(350,200);
    graph.stroke();
}
    */